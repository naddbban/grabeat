<Window x:Class="grabeat.Promo"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:grabeat"
        mc:Ignorable="d"
        Title="Voucher" Height="190.667" Width="450"  WindowStartupLocation="CenterScreen">
    <Grid HorizontalAlignment="Left" Width="440" Margin="-7,0,0,0">
        <ListBox x:Name="listBoxDaftarPromo" Margin="17,10,0,10" MouseDoubleClick="onlistBoxDaftarPromoClicked" HorizontalAlignment="Left" Width="413">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Grid Margin="0,2">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="400" />

                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Row="0" Text="{Binding diskon}" TextAlignment="Left" FontSize="16"/>


                    </Grid>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </Grid>
</Window>
